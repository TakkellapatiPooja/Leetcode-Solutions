class Solution {
    public char findTheDifference(String s, String t) {
        char r=0;
        for(int i=0;i<s.length();i++){
            r^=s.charAt(i);
            r^=t.charAt(i);
        }
        r^=t.charAt(t.length()-1);
        return r;
    }
}
